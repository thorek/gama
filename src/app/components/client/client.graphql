query ClientOverview( $id: ID!) {
  client( id: $id ) {
    id
    name
    city
    dsb
    zip
    address1
    address2
    country
    organisations {
      id
      name
      description
    }
  }
}
